# Install NodeJS in UBUNTU 20.0
sudo apt update
sudo apt install nodejs
node -v #check phiên bản
sudo apt install npm

https://nodejs.org/en/download/package-manager/#centos-fedora-and-red-hat-enterprise-linux


# run NodeJS
cd ~/Selection
node server.js